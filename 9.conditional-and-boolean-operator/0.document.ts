// 조건 연산자 (Conditional and Boolean operator)

/**
 * 조건 연산자는 특정 조건에 맞는지 알려주는 불리언 값을 리턴해 특정 조건에 해당할때 정해진 값을 발행하는 연산자이다.
 * 소스 옵저버블에서 발행하는 값을 확인히는 연산자라면 핖터링 연산자가 적합하다.
 * 이미 빌행한 값이 아닌 소스 옵저버블의 특성에 따라서 조건 자체를 분기해야할 때는 조건 연산자가 중요하다.
 */
